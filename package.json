{
    "name": "@acme/design-system",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "build": "pnpm run --parallel build",
        "dev": "pnpm run --parallel dev",
        "build:ds-css": "cd packages/ds-css && npm run build",
        "build:ds-angular": "cd packages/ds-angular && npm run build",
        "build:ds-tokens": "cd packages/ds-tokens && npm run build",
        "serve": "live-server public --watch=index.html,output.css"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "@acme/ds-css": "workspace:*",
        "@acme/ds-angular": "workspace:*",
        "npm-run-all": "^4.1.5"
    },
    "devDependencies": {
        "live-server": "^1.2.2",
        "postcss": "^8.5.6",
        "postcss-js": "^4.0.1"
    }
}